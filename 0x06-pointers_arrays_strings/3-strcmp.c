#include "main.h"
#include <string.h>

/**
 * _strcmp -> for comparing purpose
 * @s1: para1
 * @s2: para2
 * Return: Always 0
 */

int _strcmp(char *s1, char *s2)
{
	int result;
	result = strcmp(s1, s2);

	return (result);
}
